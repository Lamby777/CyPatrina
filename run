# Prepare
sudo apt update
sudo apt -y upgrade

# Install Python3
sudo apt -y install python3

# Install PAM modules
sudo apt -y install libpam-cracklib

# Install firewall
sudo apt -y install ufw gufw
